#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
long long int fac(int n)
{
if(n==0)
return 1;
return n*fac(n-1);
}
long long int comb(int n,int k)
{
    return fac(n)/(fac(k)*fac(n-k));
}
int main() {
	int T;
	cin >> T;
	while(T--)
	{
	    string S;
	    cin >> S;
	    string s1=S.substr(0,S.find('-'));
    	S.erase(0,S.find('-')+1);
    	string s2=S.substr(0,S.find('-'));
    	S.erase(0,S.find('-')+1);
    	int a=stoi(s1),b=stoi(s2),c=stoi(S);
	    int A,B,C,D;
	    cin >> A >>B>>C>>D;
	    long long int Ss=A*comb(B,a)*comb(C,b)*comb(D,c);
	    cout <<Ss<<endl;
	}
	return 0;
}
